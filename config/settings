module Settings
  # Токен доступа сообщества ВКонтакте
  ACCESS_TOKEN = '' # пока вместо него пустая строка
  
  # Настройки бота (пока такие)
  POINTS_PER_ANSWER = 10
  RATING_LIMIT = 10
  
  # Время ожидания ответа в быстром режиме (секунды)
  ANSWER_TIMEOUT = 30
end